= form_for @property do |f|
  .row
    .large-12.columns
      = f.label :name
      = f.text_field :name
  .row
    .large-12.columns
      = f.label :description
      = f.text_area :description
  = f.submit 'Update', :class => 'button small'