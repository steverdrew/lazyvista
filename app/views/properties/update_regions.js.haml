$('#property_region_id').html("#{escape_javascript(options_for_select(@regions))}");
$('#property_place_id').html("#{escape_javascript('')}");
$('#lat').val('#{escape_javascript(@country_lat)}');
$('#lng').val('#{escape_javascript(@country_lng)}');
$('#zoom').val('#{escape_javascript(@country_zoom)}');
